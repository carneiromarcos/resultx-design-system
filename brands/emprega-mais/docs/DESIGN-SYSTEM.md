# Emprega+ Design System — v0.1 (template inicial)

> ⚠️ Estrutura padrão (espelho do Electia DS). Conteúdo a popular conforme Emprega+ ganha componentes próprios. Hoje IMO e Editais herdam direto desse DS sem brand kit próprio.

---

## 0. Tokens canônicos

Fonte: `../tokens/tokens.json` + `../tokens/tokens.css` (prefixo `--emp-*`).

Mapa rápido:
- **Color:** gold + navy + light surfaces + semantic (ver BRAND-BOOK.md §4)
- **Typography:** Sora / Inter / JetBrains Mono
- **Radius:** sm 4px · md 8px · lg 12px · xl 16px

---

## 1. Componentes base

`[TBD]` — Inventariar componentes reutilizáveis (botões, cards, inputs, navbars). Referência: HTMLs em `../previews/`.

---

## 2. Color zones

`[TBD]` — Mapear superfícies dark (app padrão) vs light (Gov/Editais).

---

## 3. Tipografia (escala)

`[TBD]` — Extrair escala de `previews/brand-guidelines.html` seção "Fontes e Escala Tipográfica".

---

## 4. Motion

`[TBD]` — Ver `MOTION-GUIDE.md`.

---

## 5. Surface + elevation

`[TBD]` — Sistema de elevação (surface-1/2 + sombras).

---

## 6. Estados (hover, focus, active, disabled)

`[TBD]`

---

## 7. Acessibilidade

Meta: WCAG AA. `[TBD validar contraste de todas as combinações cor texto/fundo]`.

---

## 8. Stack recomendada

`[TBD]` — React/Vite (sites), Laravel/Livewire (app legado), Next.js (jobs-frontend).
